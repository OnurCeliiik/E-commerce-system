{
    "annotations": { "list": [] },
    "editable": true,
    "fiscalYearStartMonth": 0,
    "graphTooltip": 0,
    "id": null,
    "links": [],
    "panels": [
      {
        "datasource": { "type": "prometheus", "uid": "prometheus" },
        "fieldConfig": { "defaults": { "unit": "reqps" }, "overrides": [] },
        "gridPos": { "h": 8, "w": 12, "x": 0, "y": 0 },
        "id": 1,
        "options": { "legend": { "displayMode": "list", "placement": "bottom" } },
        "targets": [
          {
            "expr": "sum(rate(http_requests_total{job=\"product-service\",path!=\"/metrics\"}[1m])) by (method, path, status)",
            "legendFormat": "{{method}} {{path}} {{status}}",
            "refId": "A"
          }
        ],
        "title": "Product service HTTP rate",
        "type": "timeseries"
      },
      {
        "datasource": { "type": "prometheus", "uid": "prometheus" },
        "fieldConfig": { "defaults": { "unit": "s" }, "overrides": [] },
        "gridPos": { "h": 8, "w": 12, "x": 12, "y": 0 },
        "id": 2,
        "options": { "legend": { "displayMode": "list", "placement": "bottom" } },
        "targets": [
          {
            "expr": "histogram_quantile(0.95, sum(rate(http_request_duration_seconds_bucket{job=\"product-service\",path!=\"/metrics\"}[5m])) by (le, path))",
            "legendFormat": "{{path}} p95",
            "refId": "A"
          }
        ],
        "title": "Product service HTTP latency (p95)",
        "type": "timeseries"
      }
    ],
    "refresh": "10s",
    "schemaVersion": 39,
    "tags": ["product"],
    "templating": { "list": [] },
    "time": { "from": "now-15m", "to": "now" },
    "timezone": "browser",
    "title": "Product service",
    "uid": "product-overview",
    "version": 1
  }
  