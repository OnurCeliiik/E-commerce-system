# Local development overrides (not used in production).
# Copy to docker-compose.override.yml — Docker Compose merges it automatically.
#
#   cp docker-compose.override.example.yml docker-compose.override.yml
#
# Beekeeper / DBeaver connection:
#   Host: localhost | Port: 5434 | User: postgres | Password: postgres
#   Databases: user_db, product_db, inventory_db, order_db
#
# Use 5434 (not 5432) — 5432 is usually your system Postgres, not Docker.

services:
  postgres:
    ports:
      - "5434:5432"
